# Secrets and local config
.env
.env.*
*.pem
*.key
credentials*.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Node (only used for local tooling, never shipped)
node_modules/

# Foreman worker scratch
.foreman/

# Foreman worktrees
.worktrees/

# Internal docs, never public (pricing, sales scripts)
docs/interno/
